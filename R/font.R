qfont <-
  function(family = "Arial",
           pointsize = -1L,
           weight = -1L,
           italic = FALSE, ...)
{
  list(family = family,
       pointsize = pointsize,
       weight = weight,
       italic = italic)
}
